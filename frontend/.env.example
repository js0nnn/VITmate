# Base URL of the VITmate API. Leave empty when the API is served from the same
# origin (Vite dev proxy locally, or the FastAPI server in production).
VITE_API_BASE_URL=
# Where the Vite dev server forwards /api requests during development.
VITE_DEV_API_PROXY=http://127.0.0.1:8000
